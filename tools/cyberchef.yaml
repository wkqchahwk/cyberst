name: cyberchef
command: cyberchef
enabled: true
short_description: Run the cyberchef security tool.
description: 'CyberStrikeAI wrapper for `cyberchef`. Command: `cyberchef`. Review scope and arguments before execution.'
parameters:
- name: recipe
  type: string
  description: Value for `recipe`. Maps to `-Recipe`.
  required: true
  flag: -Recipe
  format: flag
- name: input
  type: string
  description: Value for `input`. Maps to `-Input`.
  required: true
  flag: -Input
  format: flag
- name: output
  type: string
  description: Value for `output`. Maps to `-Output`.
  required: false
  flag: -Output
  format: flag
- name: additional_args
  type: string
  description: Value for `additional_args`.
  required: false
  format: positional
