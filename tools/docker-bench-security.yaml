name: docker-bench-security
command: docker-bench-security
enabled: true
short_description: Run the docker-bench-security security tool.
description: 'CyberStrikeAI wrapper for `docker-bench-security`. Command: `docker-bench-security`. Review scope and arguments before execution.'
parameters:
- name: checks
  type: string
  description: Value for `checks`. Maps to `-c`.
  required: false
  flag: -c
  format: flag
- name: exclude
  type: string
  description: Value for `exclude`. Maps to `-e`.
  required: false
  flag: -e
  format: flag
- name: output_file
  type: string
  description: Value for `output_file`. Maps to `-l`.
  required: false
  flag: -l
  format: flag
- name: additional_args
  type: string
  description: Value for `additional_args`.
  required: false
  format: positional
