name: binwalk
command: binwalk
enabled: true
short_description: Run the binwalk security tool.
description: 'CyberStrikeAI wrapper for `binwalk`. Command: `binwalk`. Review scope and arguments before execution.'
parameters:
- name: file_path
  type: string
  description: Value for `file_path`.
  required: true
  position: 0
  format: positional
- name: extract
  type: bool
  description: Value for `extract`. Maps to `-e`.
  required: false
  flag: -e
  format: flag
  default: false
- name: additional_args
  type: string
  description: Value for `additional_args`.
  required: false
  format: positional
