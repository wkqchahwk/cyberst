name: masscan
command: masscan
enabled: true
short_description: Run the masscan security tool.
description: 'CyberStrikeAI wrapper for `masscan`. Command: `masscan`. Review scope and arguments before execution.'
parameters:
- name: target
  type: string
  description: Value for `target`.
  required: true
  position: 0
  format: positional
- name: ports
  type: string
  description: Value for `ports`. Maps to `-p`.
  required: false
  flag: -p
  format: flag
  default: 1-65535
- name: rate
  type: int
  description: Value for `rate`. Maps to `--rate`.
  required: false
  flag: --rate
  format: flag
  default: 1000
- name: interface
  type: string
  description: Value for `interface`. Maps to `-e`.
  required: false
  flag: -e
  format: flag
- name: banners
  type: bool
  description: Value for `banners`. Maps to `--banners`.
  required: false
  flag: --banners
  format: flag
  default: false
- name: additional_args
  type: string
  description: Value for `additional_args`.
  required: false
  format: positional
