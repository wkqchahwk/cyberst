name: wpscan
command: wpscan
enabled: true
allowed_exit_codes:
- 4
short_description: Run the wpscan security tool.
description: 'CyberStrikeAI wrapper for `wpscan`. Command: `wpscan`. Review scope and arguments before execution.'
parameters:
- name: url
  type: string
  description: Value for `url`. Maps to `--url`.
  required: true
  flag: --url
  format: flag
- name: additional_args
  type: string
  description: Value for `additional_args`.
  required: false
  format: positional
