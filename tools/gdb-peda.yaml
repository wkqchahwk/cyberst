name: gdb-peda
command: gdb
enabled: true
short_description: Run the gdb-peda security tool.
description: 'CyberStrikeAI wrapper for `gdb-peda`. Command: `gdb`. Review scope and arguments before execution.'
parameters:
- name: binary
  type: string
  description: Value for `binary`.
  required: false
  position: 0
  format: positional
- name: commands
  type: string
  description: Value for `commands`. Maps to `-ex`.
  required: false
  flag: -ex
  format: flag
- name: attach_pid
  type: int
  description: Value for `attach_pid`. Maps to `-p`.
  required: false
  flag: -p
  format: flag
- name: core_file
  type: string
  description: Value for `core_file`. Maps to `-c`.
  required: false
  flag: -c
  format: flag
- name: additional_args
  type: string
  description: Value for `additional_args`.
  required: false
  format: positional
