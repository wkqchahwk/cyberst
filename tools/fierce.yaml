name: fierce
command: fierce
enabled: true
short_description: Run the fierce security tool.
description: 'CyberStrikeAI wrapper for `fierce`. Command: `fierce`. Review scope and arguments before execution.'
parameters:
- name: domain
  type: string
  description: Value for `domain`. Maps to `-dns`.
  required: true
  flag: -dns
  format: flag
- name: dns_server
  type: string
  description: Value for `dns_server`. Maps to `-dnsserver`.
  required: false
  flag: -dnsserver
  format: flag
- name: additional_args
  type: string
  description: Value for `additional_args`.
  required: false
  format: positional
