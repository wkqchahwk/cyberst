name: kube-hunter
command: kube-hunter
enabled: true
short_description: Run the kube-hunter security tool.
description: 'CyberStrikeAI wrapper for `kube-hunter`. Command: `kube-hunter`. Review scope and arguments before execution.'
parameters:
- name: target
  type: string
  description: Value for `target`. Maps to `--remote`.
  required: false
  flag: --remote
  format: flag
- name: cidr
  type: string
  description: Value for `cidr`. Maps to `--cidr`.
  required: false
  flag: --cidr
  format: flag
- name: interface
  type: string
  description: Value for `interface`. Maps to `--interface`.
  required: false
  flag: --interface
  format: flag
- name: active
  type: bool
  description: Value for `active`. Maps to `--active`.
  required: false
  flag: --active
  format: flag
  default: false
- name: report
  type: string
  description: Value for `report`. Maps to `--report`.
  required: false
  flag: --report
  format: flag
  default: json
- name: additional_args
  type: string
  description: Value for `additional_args`.
  required: false
  format: positional
