name: arp-scan
command: arp-scan
enabled: true
short_description: Run the arp-scan security tool.
description: 'CyberStrikeAI wrapper for `arp-scan`. Command: `arp-scan`. Review scope and arguments before execution.'
parameters:
- name: target
  type: string
  description: Value for `target`.
  required: false
  position: 0
  format: positional
- name: interface
  type: string
  description: Value for `interface`. Maps to `-I`.
  required: false
  flag: -I
  format: flag
- name: local_network
  type: bool
  description: Value for `local_network`. Maps to `-l`.
  required: false
  flag: -l
  format: flag
  default: false
- name: additional_args
  type: string
  description: Value for `additional_args`.
  required: false
  format: positional
