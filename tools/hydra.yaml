name: hydra
command: hydra
enabled: true
requires_action_enabled: true
short_description: Run the hydra security tool.
description: 'CyberStrikeAI wrapper for `hydra`. Command: `hydra`. Review scope and arguments before execution.'
parameters:
- name: target
  type: string
  description: Value for `target`.
  required: true
  position: 0
  format: positional
- name: service
  type: string
  description: Value for `service`.
  required: true
  position: 1
  format: positional
- name: username
  type: string
  description: Value for `username`. Maps to `-l`.
  required: false
  flag: -l
  format: flag
- name: username_file
  type: string
  description: Value for `username_file`. Maps to `-L`.
  required: false
  flag: -L
  format: flag
- name: password
  type: string
  description: Value for `password`. Maps to `-p`.
  required: false
  flag: -p
  format: flag
- name: password_file
  type: string
  description: Value for `password_file`. Maps to `-P`.
  required: false
  flag: -P
  format: flag
- name: additional_args
  type: string
  description: Value for `additional_args`.
  required: false
  format: positional
