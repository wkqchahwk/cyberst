name: lightx
command: lightx
enabled: false
short_description: Run the lightx security tool.
description: 'CyberStrikeAI wrapper for `lightx`. Command: `lightx`. Review scope and arguments before execution.'
parameters:
- name: target
  type: string
  description: Value for `target`. Maps to `-t`.
  required: true
  flag: -t
  format: flag
