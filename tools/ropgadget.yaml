name: ropgadget
command: ROPgadget
enabled: true
short_description: Run the ropgadget security tool.
description: 'CyberStrikeAI wrapper for `ropgadget`. Command: `ROPgadget`. Review scope and arguments before execution.'
parameters:
- name: binary
  type: string
  description: Value for `binary`.
  required: true
  position: 0
  format: positional
- name: additional_args
  type: string
  description: Value for `additional_args`.
  required: false
  format: positional
