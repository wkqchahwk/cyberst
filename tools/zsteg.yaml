name: zsteg
command: zsteg
enabled: true
short_description: Run the zsteg security tool.
description: 'CyberStrikeAI wrapper for `zsteg`. Command: `zsteg`. Review scope and arguments before execution.'
parameters:
- name: file
  type: string
  description: Value for `file`.
  required: true
  position: 0
  format: positional
- name: all
  type: bool
  description: Value for `all`. Maps to `--all`.
  required: false
  flag: --all
  format: flag
- name: lsb
  type: bool
  description: Value for `lsb`. Maps to `--lsb`.
  required: false
  flag: --lsb
  format: flag
- name: additional_args
  type: string
  description: Value for `additional_args`.
  required: false
  format: positional
