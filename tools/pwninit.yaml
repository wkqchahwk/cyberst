name: pwninit
command: pwninit
enabled: true
short_description: Run the pwninit security tool.
description: 'CyberStrikeAI wrapper for `pwninit`. Command: `pwninit`. Review scope and arguments before execution.'
parameters:
- name: binary
  type: string
  description: Value for `binary`.
  required: true
  position: 0
  format: positional
- name: libc
  type: string
  description: Value for `libc`. Maps to `--libc`.
  required: false
  flag: --libc
  format: flag
- name: ld
  type: string
  description: Value for `ld`. Maps to `--ld`.
  required: false
  flag: --ld
  format: flag
- name: template_type
  type: string
  description: Value for `template_type`. Maps to `--template`.
  required: false
  flag: --template
  format: flag
  default: python
- name: additional_args
  type: string
  description: Value for `additional_args`.
  required: false
  format: positional
