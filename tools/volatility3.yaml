name: volatility3
command: volatility3
enabled: true
short_description: Run the volatility3 security tool.
description: 'CyberStrikeAI wrapper for `volatility3`. Command: `volatility3`. Review scope and arguments before execution.'
parameters:
- name: memory_file
  type: string
  description: Value for `memory_file`. Maps to `-f`.
  required: true
  flag: -f
  format: flag
- name: plugin
  type: string
  description: Value for `plugin`.
  required: true
  position: 0
  format: positional
- name: output_file
  type: string
  description: Value for `output_file`. Maps to `-o`.
  required: false
  flag: -o
  format: flag
- name: additional_args
  type: string
  description: Value for `additional_args`.
  required: false
  format: positional
