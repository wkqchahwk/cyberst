name: linpeas
command: linpeas.sh
enabled: true
requires_action_enabled: true
short_description: Run the linpeas security tool.
description: 'CyberStrikeAI wrapper for `linpeas`. Command: `linpeas.sh`. Review scope and arguments before execution.'
parameters:
- name: output
  type: string
  description: Value for `output`. Maps to `-o`.
  required: false
  flag: -o
  format: flag
- name: fast
  type: bool
  description: Value for `fast`. Maps to `-fast`.
  required: false
  flag: -fast
  format: flag
- name: additional_args
  type: string
  description: Value for `additional_args`.
  required: false
  format: positional
