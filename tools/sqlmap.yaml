name: sqlmap
command: sqlmap
enabled: true
requires_action_enabled: true
short_description: Run the sqlmap security tool.
description: 'CyberStrikeAI wrapper for `sqlmap`. Command: `sqlmap`. Review scope and arguments before execution.'
parameters:
- name: url
  type: string
  description: Value for `url`. Maps to `-u`.
  required: true
  flag: -u
  format: flag
- name: batch
  type: bool
  description: Value for `batch`. Maps to `--batch`.
  required: false
  default: true
  flag: --batch
  format: flag
- name: level
  type: int
  description: Value for `level`. Maps to `--level`.
  required: false
  default: 3
  flag: --level
  format: combined
- name: data
  type: string
  description: Value for `data`. Maps to `--data`.
  required: false
  flag: --data
  format: flag
- name: cookie
  type: string
  description: Value for `cookie`. Maps to `--cookie`.
  required: false
  flag: --cookie
  format: flag
- name: additional_args
  type: string
  description: Value for `additional_args`.
  required: false
  format: positional
