name: stegsolve
command: java
args:
- -jar
enabled: true
short_description: Run the stegsolve security tool.
description: 'CyberStrikeAI wrapper for `stegsolve`. Command: `java`. Review scope and arguments before execution.'
parameters:
- name: jar_file
  type: string
  description: Value for `jar_file`.
  required: true
  position: 0
  format: positional
- name: image
  type: string
  description: Value for `image`.
  required: false
  position: 1
  format: positional
- name: additional_args
  type: string
  description: Value for `additional_args`.
  required: false
  format: positional
