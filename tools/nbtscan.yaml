name: nbtscan
command: nbtscan
enabled: true
short_description: Run the nbtscan security tool.
description: 'CyberStrikeAI wrapper for `nbtscan`. Command: `nbtscan`. Review scope and arguments before execution.'
parameters:
- name: target
  type: string
  description: Value for `target`.
  required: true
  position: 0
  format: positional
- name: verbose
  type: bool
  description: Value for `verbose`. Maps to `-v`.
  required: false
  flag: -v
  format: flag
  default: false
- name: timeout
  type: int
  description: Value for `timeout`. Maps to `-t`.
  required: false
  flag: -t
  format: flag
  default: 2
- name: additional_args
  type: string
  description: Value for `additional_args`.
  required: false
  format: positional
