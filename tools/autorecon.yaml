name: autorecon
command: autorecon
enabled: true
short_description: Run the autorecon security tool.
description: 'CyberStrikeAI wrapper for `autorecon`. Command: `autorecon`. Review scope and arguments before execution.'
parameters:
- name: target
  type: string
  description: Value for `target`.
  required: true
  position: 0
  format: positional
- name: output_dir
  type: string
  description: Value for `output_dir`. Maps to `-o`.
  required: false
  flag: -o
  format: flag
  default: /tmp/autorecon
- name: additional_args
  type: string
  description: Value for `additional_args`.
  required: false
  format: positional
