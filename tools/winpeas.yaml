name: winpeas
command: winPEAS.exe
enabled: true
requires_action_enabled: true
short_description: Run the winpeas security tool.
description: 'CyberStrikeAI wrapper for `winpeas`. Command: `winPEAS.exe`. Review scope and arguments before execution.'
parameters:
- name: quiet
  type: bool
  description: Value for `quiet`. Maps to `-q`.
  required: false
  flag: -q
  format: flag
- name: notcolor
  type: bool
  description: Value for `notcolor`. Maps to `-notcolor`.
  required: false
  flag: -notcolor
  format: flag
- name: additional_args
  type: string
  description: Value for `additional_args`.
  required: false
  format: positional
