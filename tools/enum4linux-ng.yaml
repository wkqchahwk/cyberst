name: enum4linux-ng
command: enum4linux-ng
enabled: true
short_description: Run the enum4linux-ng security tool.
description: 'CyberStrikeAI wrapper for `enum4linux-ng`. Command: `enum4linux-ng`. Review scope and arguments before execution.'
parameters:
- name: target
  type: string
  description: Value for `target`.
  required: true
  position: 0
  format: positional
- name: username
  type: string
  description: Value for `username`. Maps to `-u`.
  required: false
  flag: -u
  format: flag
- name: password
  type: string
  description: Value for `password`. Maps to `-p`.
  required: false
  flag: -p
  format: flag
- name: domain
  type: string
  description: Value for `domain`. Maps to `-d`.
  required: false
  flag: -d
  format: flag
- name: shares
  type: bool
  description: Value for `shares`. Maps to `-S`.
  required: false
  flag: -S
  format: flag
  default: true
- name: users
  type: bool
  description: Value for `users`. Maps to `-U`.
  required: false
  flag: -U
  format: flag
  default: true
- name: groups
  type: bool
  description: Value for `groups`. Maps to `-G`.
  required: false
  flag: -G
  format: flag
  default: true
- name: policy
  type: bool
  description: Value for `policy`. Maps to `-P`.
  required: false
  flag: -P
  format: flag
  default: true
- name: additional_args
  type: string
  description: Value for `additional_args`.
  required: false
  format: positional
