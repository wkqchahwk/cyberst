name: xxd
command: xxd
enabled: true
short_description: Run the xxd security tool.
description: 'CyberStrikeAI wrapper for `xxd`. Command: `xxd`. Review scope and arguments before execution.'
parameters:
- name: file_path
  type: string
  description: Value for `file_path`.
  required: true
  position: 0
  format: positional
- name: offset
  type: string
  description: Value for `offset`. Maps to `-s`.
  required: false
  flag: -s
  format: flag
  default: '0'
- name: length
  type: string
  description: Value for `length`. Maps to `-l`.
  required: false
  flag: -l
  format: flag
- name: additional_args
  type: string
  description: Value for `additional_args`.
  required: false
  format: positional
