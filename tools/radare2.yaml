name: radare2
command: r2
enabled: true
short_description: Run the radare2 security tool.
description: 'CyberStrikeAI wrapper for `radare2`. Command: `r2`. Review scope and arguments before execution.'
parameters:
- name: target
  type: string
  description: Value for `target`.
  required: true
  position: 1
  format: positional
- name: commands
  type: string
  description: Value for `commands`. Maps to `-c`.
  required: false
  flag: -c
  format: flag
- name: script_file
  type: string
  description: Value for `script_file`. Maps to `-i`.
  required: false
  flag: -i
  format: flag
- name: arch
  type: string
  description: Value for `arch`. Maps to `-a`.
  required: false
  flag: -a
  format: flag
- name: bits
  type: int
  description: Value for `bits`. Maps to `-b`.
  required: false
  flag: -b
  format: flag
- name: auto_analyze
  type: bool
  description: Value for `auto_analyze`. Maps to `-A`.
  required: false
  flag: -A
  format: flag
  default: false
- name: debug
  type: bool
  description: Value for `debug`. Maps to `-d`.
  required: false
  flag: -d
  format: flag
  default: false
- name: quiet
  type: bool
  description: Value for `quiet`. Maps to `-q`.
  required: false
  flag: -q
  format: flag
  default: false
- name: seek
  type: string
  description: Value for `seek`. Maps to `-s`.
  required: false
  flag: -s
  format: flag
- name: base_address
  type: string
  description: Value for `base_address`. Maps to `-B`.
  required: false
  flag: -B
  format: flag
- name: eval
  type: string
  description: Value for `eval`. Maps to `-e`.
  required: false
  flag: -e
  format: flag
- name: project
  type: string
  description: Value for `project`. Maps to `-p`.
  required: false
  flag: -p
  format: flag
- name: additional_args
  type: string
  description: Value for `additional_args`.
  required: false
  format: positional
