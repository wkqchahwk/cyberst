name: feroxbuster
command: feroxbuster
enabled: true
short_description: Run the feroxbuster security tool.
description: 'CyberStrikeAI wrapper for `feroxbuster`. Command: `feroxbuster`. Review scope and arguments before execution.'
parameters:
- name: url
  type: string
  description: Value for `url`. Maps to `-u`.
  required: true
  flag: -u
  format: flag
- name: wordlist
  type: string
  description: Value for `wordlist`. Maps to `-w`.
  required: false
  flag: -w
  format: flag
- name: threads
  type: int
  description: Value for `threads`. Maps to `-t`.
  required: false
  flag: -t
  format: flag
  default: 10
- name: additional_args
  type: string
  description: Value for `additional_args`.
  required: false
  format: positional
