name: hashpump
command: hashpump
enabled: true
short_description: Run the hashpump security tool.
description: 'CyberStrikeAI wrapper for `hashpump`. Command: `hashpump`. Review scope and arguments before execution.'
parameters:
- name: signature
  type: string
  description: Value for `signature`. Maps to `-s`.
  required: true
  flag: -s
  format: flag
- name: data
  type: string
  description: Value for `data`. Maps to `-d`.
  required: true
  flag: -d
  format: flag
- name: key_length
  type: int
  description: Value for `key_length`. Maps to `-k`.
  required: true
  flag: -k
  format: flag
- name: append_data
  type: string
  description: Value for `append_data`. Maps to `-a`.
  required: true
  flag: -a
  format: flag
- name: additional_args
  type: string
  description: Value for `additional_args`.
  required: false
  format: positional
