name: wafw00f
command: wafw00f
enabled: true
short_description: Run the wafw00f security tool.
description: 'CyberStrikeAI wrapper for `wafw00f`. Command: `wafw00f`. Review scope and arguments before execution.'
parameters:
- name: target
  type: string
  description: Value for `target`.
  required: true
  position: 0
  format: positional
- name: additional_args
  type: string
  description: Value for `additional_args`.
  required: false
  format: positional
