name: dalfox
command: dalfox
args:
- url
enabled: true
short_description: Run the dalfox security tool.
description: 'CyberStrikeAI wrapper for `dalfox`. Command: `dalfox`. Review scope and arguments before execution.'
parameters:
- name: url
  type: string
  description: Value for `url`.
  required: true
  position: 0
  format: positional
- name: pipe_mode
  type: bool
  description: Value for `pipe_mode`. Maps to `--pipe`.
  required: false
  flag: --pipe
  format: flag
  default: false
- name: blind
  type: string
  description: Value for `blind`. Maps to `--blind`.
  required: false
  flag: --blind
  format: flag
- name: mining_dom
  type: bool
  description: Value for `mining_dom`. Maps to `--mining-dom`.
  required: false
  flag: --mining-dom
  format: flag
  default: true
- name: mining_dict
  type: bool
  description: Value for `mining_dict`. Maps to `--mining-dict`.
  required: false
  flag: --mining-dict
  format: flag
  default: true
- name: additional_args
  type: string
  description: Value for `additional_args`.
  required: false
  format: positional
