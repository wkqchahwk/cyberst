name: exec
command: sh
args:
- -c
enabled: true
short_description: Run the exec security tool.
description: 'CyberStrikeAI wrapper for `exec`. Command: `sh`. Review scope and arguments before execution.'
parameters:
- name: command
  type: string
  description: Value for `command`.
  required: true
  position: 0
  format: positional
- name: shell
  type: string
  description: Value for `shell`.
  required: false
  default: sh
- name: workdir
  type: string
  description: Value for `workdir`.
  required: false
- name: additional_args
  type: string
  description: Value for `additional_args`.
  required: false
  format: positional
