name: exiftool
command: exiftool
enabled: true
short_description: Run the exiftool security tool.
description: 'CyberStrikeAI wrapper for `exiftool`. Command: `exiftool`. Review scope and arguments before execution.'
parameters:
- name: file_path
  type: string
  description: Value for `file_path`.
  required: true
  position: 0
  format: positional
- name: output_json
  type: bool
  description: Value for `output_json`. Maps to `-json`.
  required: false
  flag: -json
  format: flag
  default: false
- name: output_xml
  type: bool
  description: Value for `output_xml`. Maps to `-X`.
  required: false
  flag: -X
  format: flag
  default: false
- name: output_csv
  type: bool
  description: Value for `output_csv`. Maps to `-csv`.
  required: false
  flag: -csv
  format: flag
  default: false
- name: tags
  type: string
  description: Value for `tags`.
  required: false
  format: template
  template: -{value}
- name: additional_args
  type: string
  description: Value for `additional_args`.
  required: false
  format: positional
