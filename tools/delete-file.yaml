name: delete-file
command: rm
enabled: true
short_description: Run the delete-file security tool.
description: 'CyberStrikeAI wrapper for `delete-file`. Command: `rm`. Review scope and arguments before execution.'
parameters:
- name: filename
  type: string
  description: Value for `filename`.
  required: true
  position: 0
  format: positional
- name: recursive
  type: bool
  description: Value for `recursive`. Maps to `-r`.
  required: false
  flag: -r
  format: flag
  default: false
- name: additional_args
  type: string
  description: Value for `additional_args`.
  required: false
  format: positional
