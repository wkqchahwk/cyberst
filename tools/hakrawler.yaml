name: hakrawler
command: hakrawler
enabled: true
short_description: Run the hakrawler security tool.
description: 'CyberStrikeAI wrapper for `hakrawler`. Command: `hakrawler`. Review scope and arguments before execution.'
parameters:
- name: url
  type: string
  description: Value for `url`. Maps to `-url`.
  required: true
  flag: -url
  format: flag
- name: depth
  type: int
  description: Value for `depth`. Maps to `-d`.
  required: false
  flag: -d
  format: flag
  default: 2
- name: forms
  type: bool
  description: Value for `forms`. Maps to `-forms`.
  required: false
  flag: -forms
  format: flag
  default: true
- name: additional_args
  type: string
  description: Value for `additional_args`.
  required: false
  format: positional
