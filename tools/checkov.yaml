name: checkov
command: checkov
enabled: true
short_description: Run the checkov security tool.
description: 'CyberStrikeAI wrapper for `checkov`. Command: `checkov`. Review scope and arguments before execution.'
parameters:
- name: directory
  type: string
  description: Value for `directory`. Maps to `-d`.
  required: false
  flag: -d
  format: flag
  default: .
- name: framework
  type: string
  description: Value for `framework`. Maps to `--framework`.
  required: false
  flag: --framework
  format: flag
- name: check
  type: string
  description: Value for `check`. Maps to `--check`.
  required: false
  flag: --check
  format: flag
- name: output_format
  type: string
  description: Value for `output_format`. Maps to `--output`.
  required: false
  flag: --output
  format: flag
  default: json
- name: additional_args
  type: string
  description: Value for `additional_args`.
  required: false
  format: positional
