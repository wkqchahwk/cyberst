name: pacu
command: pacu
enabled: false
requires_action_enabled: true
short_description: Run the pacu security tool.
description: 'CyberStrikeAI wrapper for `pacu`. Command: `pacu`. Review scope and arguments before execution.'
parameters:
- name: session_name
  type: string
  description: Value for `session_name`. Maps to `--session`.
  required: false
  flag: --session
  format: flag
  default: hexstrike_session
- name: modules
  type: string
  description: Value for `modules`. Maps to `--modules`.
  required: false
  flag: --modules
  format: flag
- name: regions
  type: string
  description: Value for `regions`. Maps to `--regions`.
  required: false
  flag: --regions
  format: flag
- name: additional_args
  type: string
  description: Value for `additional_args`.
  required: false
  format: positional
