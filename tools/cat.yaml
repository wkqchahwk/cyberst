name: cat
enabled: true
command: cat
short_description: Run the cat security tool.
description: 'CyberStrikeAI wrapper for `cat`. Command: `cat`. Review scope and arguments before execution.'
parameters:
- name: file
  type: string
  description: Value for `file`.
  required: true
  format: positional
  position: 0
