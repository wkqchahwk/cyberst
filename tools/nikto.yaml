name: nikto
command: nikto
enabled: true
allowed_exit_codes:
- 1
short_description: Run the nikto security tool.
description: 'CyberStrikeAI wrapper for `nikto`. Command: `nikto`. Review scope and arguments before execution.'
parameters:
- name: target
  type: string
  description: Value for `target`. Maps to `-h`.
  required: true
  flag: -h
  format: flag
- name: additional_args
  type: string
  description: Value for `additional_args`.
  required: false
  format: positional
