name: prowler
command: prowler
enabled: true
short_description: Run the prowler security tool.
description: 'CyberStrikeAI wrapper for `prowler`. Command: `prowler`. Review scope and arguments before execution.'
parameters:
- name: provider
  type: string
  description: Value for `provider`.
  required: false
  default: aws
  position: 0
  format: positional
- name: profile
  type: string
  description: Value for `profile`. Maps to `-p`.
  required: false
  flag: -p
  format: flag
  default: default
- name: region
  type: string
  description: Value for `region`. Maps to `-r`.
  required: false
  flag: -r
  format: flag
- name: checks
  type: string
  description: Value for `checks`. Maps to `-c`.
  required: false
  flag: -c
  format: flag
- name: output_format
  type: string
  description: Value for `output_format`. Maps to `-M`.
  required: false
  flag: -M
  format: flag
  default: json
- name: additional_args
  type: string
  description: Value for `additional_args`.
  required: false
  format: positional
