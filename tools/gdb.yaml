name: gdb
command: gdb
enabled: true
short_description: Run the gdb security tool.
description: 'CyberStrikeAI wrapper for `gdb`. Command: `gdb`. Review scope and arguments before execution.'
parameters:
- name: binary
  type: string
  description: Value for `binary`.
  required: true
  position: 0
  format: positional
- name: commands
  type: string
  description: Value for `commands`. Maps to `-ex`.
  required: false
  flag: -ex
  format: flag
- name: script_file
  type: string
  description: Value for `script_file`. Maps to `-x`.
  required: false
  flag: -x
  format: flag
- name: additional_args
  type: string
  description: Value for `additional_args`.
  required: false
  format: positional
