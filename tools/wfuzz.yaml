name: wfuzz
command: wfuzz
enabled: true
short_description: Run the wfuzz security tool.
description: 'CyberStrikeAI wrapper for `wfuzz`. Command: `wfuzz`. Review scope and arguments before execution.'
parameters:
- name: url
  type: string
  description: Value for `url`. Maps to `-u`.
  required: true
  flag: -u
  format: flag
- name: wordlist
  type: string
  description: Value for `wordlist`. Maps to `-w`.
  required: false
  flag: -w
  format: flag
- name: additional_args
  type: string
  description: Value for `additional_args`.
  required: false
  format: positional
