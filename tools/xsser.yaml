name: xsser
command: xsser
enabled: true
short_description: Run the xsser security tool.
description: 'CyberStrikeAI wrapper for `xsser`. Command: `xsser`. Review scope and arguments before execution.'
parameters:
- name: url
  type: string
  description: Value for `url`. Maps to `--url`.
  required: true
  flag: --url
  format: flag
- name: params
  type: string
  description: Value for `params`. Maps to `--Fp`.
  required: false
  flag: --Fp
  format: flag
- name: additional_args
  type: string
  description: Value for `additional_args`.
  required: false
  format: positional
