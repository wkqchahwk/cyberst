name: waybackurls
command: waybackurls
enabled: true
short_description: Run the waybackurls security tool.
description: 'CyberStrikeAI wrapper for `waybackurls`. Command: `waybackurls`. Review scope and arguments before execution.'
parameters:
- name: domain
  type: string
  description: Value for `domain`.
  required: true
  position: 0
  format: positional
- name: get_versions
  type: bool
  description: Value for `get_versions`. Maps to `-get-versions`.
  required: false
  flag: -get-versions
  format: flag
  default: false
- name: no_subs
  type: bool
  description: Value for `no_subs`. Maps to `-no-subs`.
  required: false
  flag: -no-subs
  format: flag
  default: false
- name: additional_args
  type: string
  description: Value for `additional_args`.
  required: false
  format: positional
