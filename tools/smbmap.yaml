name: smbmap
command: smbmap
enabled: true
short_description: Run the smbmap security tool.
description: 'CyberStrikeAI wrapper for `smbmap`. Command: `smbmap`. Review scope and arguments before execution.'
parameters:
- name: target
  type: string
  description: Value for `target`. Maps to `-H`.
  required: true
  flag: -H
  format: flag
- name: username
  type: string
  description: Value for `username`. Maps to `-u`.
  required: false
  flag: -u
  format: flag
- name: password
  type: string
  description: Value for `password`. Maps to `-p`.
  required: false
  flag: -p
  format: flag
- name: domain
  type: string
  description: Value for `domain`. Maps to `-d`.
  required: false
  flag: -d
  format: flag
- name: additional_args
  type: string
  description: Value for `additional_args`.
  required: false
  format: positional
