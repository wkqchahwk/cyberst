name: nuclei
command: nuclei
enabled: true
short_description: Run the nuclei security tool.
description: 'CyberStrikeAI wrapper for `nuclei`. Command: `nuclei`. Review scope and arguments before execution.'
parameters:
- name: target
  type: string
  description: Value for `target`. Maps to `-u`.
  required: true
  flag: -u
  format: flag
- name: severity
  type: string
  description: Value for `severity`. Maps to `-s`.
  required: false
  flag: -s
  format: flag
- name: tags
  type: string
  description: Value for `tags`. Maps to `-tags`.
  required: false
  flag: -tags
  format: flag
- name: template
  type: string
  description: Value for `template`. Maps to `-t`.
  required: false
  flag: -t
  format: flag
- name: additional_args
  type: string
  description: Value for `additional_args`.
  required: false
  format: positional
