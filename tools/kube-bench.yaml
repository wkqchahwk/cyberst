name: kube-bench
command: kube-bench
enabled: true
short_description: Run the kube-bench security tool.
description: 'CyberStrikeAI wrapper for `kube-bench`. Command: `kube-bench`. Review scope and arguments before execution.'
parameters:
- name: targets
  type: string
  description: Value for `targets`. Maps to `--targets`.
  required: false
  flag: --targets
  format: flag
- name: version
  type: string
  description: Value for `version`. Maps to `--version`.
  required: false
  flag: --version
  format: flag
- name: config_dir
  type: string
  description: Value for `config_dir`. Maps to `--config-dir`.
  required: false
  flag: --config-dir
  format: flag
- name: output_format
  type: string
  description: Value for `output_format`. Maps to `--output`.
  required: false
  flag: --output
  format: flag
  default: json
- name: additional_args
  type: string
  description: Value for `additional_args`.
  required: false
  format: positional
