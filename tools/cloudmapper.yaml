name: cloudmapper
command: cloudmapper
enabled: true
short_description: Run the cloudmapper security tool.
description: 'CyberStrikeAI wrapper for `cloudmapper`. Command: `cloudmapper`. Review scope and arguments before execution.'
parameters:
- name: action
  type: string
  description: Value for `action`.
  required: false
  default: collect
  position: 0
  format: positional
- name: account
  type: string
  description: Value for `account`. Maps to `--account`.
  required: false
  flag: --account
  format: flag
- name: config
  type: string
  description: Value for `config`. Maps to `--config`.
  required: false
  flag: --config
  format: flag
  default: config.json
- name: additional_args
  type: string
  description: Value for `additional_args`.
  required: false
  format: positional
