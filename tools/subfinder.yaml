name: subfinder
command: subfinder
enabled: true
short_description: Run the subfinder security tool.
description: 'CyberStrikeAI wrapper for `subfinder`. Command: `subfinder`. Review scope and arguments before execution.'
parameters:
- name: domain
  type: string
  description: Value for `domain`. Maps to `-d`.
  required: true
  flag: -d
  format: flag
- name: silent
  type: bool
  description: Value for `silent`. Maps to `-silent`.
  required: false
  default: true
  flag: -silent
  format: flag
- name: all_sources
  type: bool
  description: Value for `all_sources`. Maps to `-all`.
  required: false
  default: false
  flag: -all
  format: flag
- name: additional_args
  type: string
  description: Value for `additional_args`.
  required: false
  format: positional
