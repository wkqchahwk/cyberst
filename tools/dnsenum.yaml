name: dnsenum
command: dnsenum
enabled: true
short_description: Run the dnsenum security tool.
description: 'CyberStrikeAI wrapper for `dnsenum`. Command: `dnsenum`. Review scope and arguments before execution.'
parameters:
- name: domain
  type: string
  description: Value for `domain`.
  required: true
  position: 0
  format: positional
- name: dns_server
  type: string
  description: Value for `dns_server`. Maps to `-n`.
  required: false
  flag: -n
  format: flag
- name: wordlist
  type: string
  description: Value for `wordlist`. Maps to `-f`.
  required: false
  flag: -f
  format: flag
- name: additional_args
  type: string
  description: Value for `additional_args`.
  required: false
  format: positional
