name: x8
command: x8
enabled: true
short_description: Run the x8 security tool.
description: 'CyberStrikeAI wrapper for `x8`. Command: `x8`. Review scope and arguments before execution.'
parameters:
- name: url
  type: string
  description: Value for `url`. Maps to `--url`.
  required: true
  flag: --url
  format: flag
- name: wordlist
  type: string
  description: Value for `wordlist`. Maps to `--wordlist`.
  required: false
  flag: --wordlist
  format: flag
  default: /usr/share/wordlists/x8/params.txt
- name: method
  type: string
  description: Value for `method`. Maps to `--method`.
  required: false
  flag: --method
  format: flag
  default: GET
- name: body
  type: string
  description: Value for `body`. Maps to `--body`.
  required: false
  flag: --body
  format: flag
- name: headers
  type: string
  description: Value for `headers`. Maps to `--headers`.
  required: false
  flag: --headers
  format: flag
- name: additional_args
  type: string
  description: Value for `additional_args`.
  required: false
  format: positional
