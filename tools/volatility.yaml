name: volatility
command: volatility
enabled: true
short_description: Run the volatility security tool.
description: 'CyberStrikeAI wrapper for `volatility`. Command: `volatility`. Review scope and arguments before execution.'
parameters:
- name: memory_file
  type: string
  description: Value for `memory_file`. Maps to `-f`.
  required: true
  flag: -f
  format: flag
- name: plugin
  type: string
  description: Value for `plugin`.
  required: true
  position: 0
  format: positional
- name: profile
  type: string
  description: Value for `profile`. Maps to `--profile`.
  required: false
  flag: --profile
  format: flag
- name: additional_args
  type: string
  description: Value for `additional_args`.
  required: false
  format: positional
