name: john
command: john
enabled: true
short_description: Run the john security tool.
description: 'CyberStrikeAI wrapper for `john`. Command: `john`. Review scope and arguments before execution.'
parameters:
- name: hash_file
  type: string
  description: Value for `hash_file`.
  required: true
  position: 0
  format: positional
- name: wordlist
  type: string
  description: Value for `wordlist`. Maps to `--wordlist`.
  required: false
  default: /usr/share/wordlists/rockyou.txt
  flag: --wordlist
  format: flag
- name: format_type
  type: string
  description: Value for `format_type`. Maps to `--format`.
  required: false
  flag: --format
  format: flag
- name: additional_args
  type: string
  description: Value for `additional_args`.
  required: false
  format: positional
