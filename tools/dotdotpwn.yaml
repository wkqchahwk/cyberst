name: dotdotpwn
command: dotdotpwn
enabled: true
short_description: Run the dotdotpwn security tool.
description: 'CyberStrikeAI wrapper for `dotdotpwn`. Command: `dotdotpwn`. Review scope and arguments before execution.'
parameters:
- name: target
  type: string
  description: Value for `target`. Maps to `-h`.
  required: true
  flag: -h
  format: flag
- name: module
  type: string
  description: Value for `module`. Maps to `-m`.
  required: false
  flag: -m
  format: flag
  default: http
- name: additional_args
  type: string
  description: Value for `additional_args`.
  required: false
  format: positional
