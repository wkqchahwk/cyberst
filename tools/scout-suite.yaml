name: scout-suite
command: scout
enabled: true
short_description: Run the scout-suite security tool.
description: 'CyberStrikeAI wrapper for `scout-suite`. Command: `scout`. Review scope and arguments before execution.'
parameters:
- name: provider
  type: string
  description: Value for `provider`. Maps to `--provider`.
  required: false
  flag: --provider
  format: flag
  default: aws
- name: profile
  type: string
  description: Value for `profile`. Maps to `--profile`.
  required: false
  flag: --profile
  format: flag
  default: default
- name: report_dir
  type: string
  description: Value for `report_dir`. Maps to `--report-dir`.
  required: false
  flag: --report-dir
  format: flag
  default: /tmp/scout-suite
- name: services
  type: string
  description: Value for `services`. Maps to `--services`.
  required: false
  flag: --services
  format: flag
- name: additional_args
  type: string
  description: Value for `additional_args`.
  required: false
  format: positional
