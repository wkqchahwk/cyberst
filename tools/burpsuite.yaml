name: burpsuite
command: burpsuite
enabled: true
short_description: Run the burpsuite security tool.
description: 'CyberStrikeAI wrapper for `burpsuite`. Command: `burpsuite`. Review scope and arguments before execution.'
parameters:
- name: project_file
  type: string
  description: Value for `project_file`. Maps to `--project-file`.
  required: false
  flag: --project-file
  format: flag
- name: config_file
  type: string
  description: Value for `config_file`. Maps to `--config-file`.
  required: false
  flag: --config-file
  format: flag
- name: user_config_file
  type: string
  description: Value for `user_config_file`. Maps to `--user-config-file`.
  required: false
  flag: --user-config-file
  format: flag
- name: headless
  type: bool
  description: Value for `headless`. Maps to `--headless`.
  required: false
  flag: --headless
  format: flag
  default: false
- name: additional_args
  type: string
  description: Value for `additional_args`.
  required: false
  format: positional
