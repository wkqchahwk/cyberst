name: strings
command: strings
enabled: true
short_description: Run the strings security tool.
description: 'CyberStrikeAI wrapper for `strings`. Command: `strings`. Review scope and arguments before execution.'
parameters:
- name: file_path
  type: string
  description: Value for `file_path`.
  required: true
  position: 0
  format: positional
- name: min_len
  type: int
  description: Value for `min_len`. Maps to `-n`.
  required: false
  flag: -n
  format: flag
  default: 4
- name: additional_args
  type: string
  description: Value for `additional_args`.
  required: false
  format: positional
