name: one-gadget
command: one_gadget
enabled: true
short_description: Run the one-gadget security tool.
description: 'CyberStrikeAI wrapper for `one-gadget`. Command: `one_gadget`. Review scope and arguments before execution.'
parameters:
- name: libc_path
  type: string
  description: Value for `libc_path`.
  required: true
  position: 0
  format: positional
- name: level
  type: int
  description: Value for `level`. Maps to `-l`.
  required: false
  flag: -l
  format: flag
  default: 1
- name: additional_args
  type: string
  description: Value for `additional_args`.
  required: false
  format: positional
