name: msfvenom
command: msfvenom
enabled: true
requires_action_enabled: true
short_description: Run the msfvenom security tool.
description: 'CyberStrikeAI wrapper for `msfvenom`. Command: `msfvenom`. Review scope and arguments before execution.'
parameters:
- name: payload
  type: string
  description: Value for `payload`. Maps to `-p`.
  required: true
  flag: -p
  format: flag
- name: format_type
  type: string
  description: Value for `format_type`. Maps to `-f`.
  required: false
  flag: -f
  format: flag
- name: output_file
  type: string
  description: Value for `output_file`. Maps to `-o`.
  required: false
  flag: -o
  format: flag
- name: encoder
  type: string
  description: Value for `encoder`. Maps to `-e`.
  required: false
  flag: -e
  format: flag
- name: iterations
  type: string
  description: Value for `iterations`. Maps to `-i`.
  required: false
  flag: -i
  format: flag
- name: additional_args
  type: string
  description: Value for `additional_args`.
  required: false
  format: positional
