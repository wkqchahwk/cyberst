name: dirb
command: dirb
enabled: true
short_description: Run the dirb security tool.
description: 'CyberStrikeAI wrapper for `dirb`. Command: `dirb`. Review scope and arguments before execution.'
parameters:
- name: url
  type: string
  description: Value for `url`.
  required: true
  position: 0
  format: positional
- name: wordlist
  type: string
  description: Value for `wordlist`.
  required: false
  position: 1
  format: positional
- name: additional_args
  type: string
  description: Value for `additional_args`.
  required: false
  format: positional
