name: fcrackzip
command: fcrackzip
enabled: true
short_description: Run the fcrackzip security tool.
description: 'CyberStrikeAI wrapper for `fcrackzip`. Command: `fcrackzip`. Review scope and arguments before execution.'
parameters:
- name: file
  type: string
  description: Value for `file`.
  required: true
  position: 0
  format: positional
- name: dictionary_mode
  type: bool
  description: Value for `dictionary_mode`. Maps to `-D`.
  required: false
  flag: -D
  format: flag
  default: false
- name: dictionary
  type: string
  description: Value for `dictionary`. Maps to `-p`.
  required: false
  flag: -p
  format: flag
- name: bruteforce
  type: bool
  description: Value for `bruteforce`. Maps to `-b`.
  required: false
  flag: -b
  format: flag
- name: charset
  type: string
  description: Value for `charset`. Maps to `-c`.
  required: false
  flag: -c
  format: flag
- name: length_range
  type: string
  description: Value for `length_range`. Maps to `-l`.
  required: false
  flag: -l
  format: flag
- name: additional_args
  type: string
  description: Value for `additional_args`.
  required: false
  format: positional
