name: amass
command: amass
enabled: true
short_description: Run the amass security tool.
description: 'CyberStrikeAI wrapper for `amass`. Command: `amass`. Review scope and arguments before execution.'
parameters:
- name: mode
  type: string
  description: Value for `mode`.
  required: false
  default: enum
  position: 0
  format: positional
  options:
  - enum
  - intel
  - viz
- name: domain
  type: string
  description: Value for `domain`. Maps to `-d`.
  required: true
  flag: -d
  format: flag
- name: additional_args
  type: string
  description: Value for `additional_args`.
  required: false
  format: positional
