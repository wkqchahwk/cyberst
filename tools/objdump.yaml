name: objdump
command: objdump
enabled: true
short_description: Run the objdump security tool.
description: 'CyberStrikeAI wrapper for `objdump`. Command: `objdump`. Review scope and arguments before execution.'
parameters:
- name: binary
  type: string
  description: Value for `binary`.
  required: true
  position: 0
  format: positional
- name: disassemble
  type: bool
  description: Value for `disassemble`. Maps to `-d`.
  required: false
  flag: -d
  format: flag
  default: true
- name: additional_args
  type: string
  description: Value for `additional_args`.
  required: false
  format: positional
