name: pdfcrack
command: pdfcrack
enabled: true
short_description: Run the pdfcrack security tool.
description: 'CyberStrikeAI wrapper for `pdfcrack`. Command: `pdfcrack`. Review scope and arguments before execution.'
parameters:
- name: file
  type: string
  description: Value for `file`.
  required: true
  position: 0
  format: positional
- name: wordlist
  type: string
  description: Value for `wordlist`. Maps to `-w`.
  required: false
  flag: -w
  format: flag
- name: min_length
  type: int
  description: Value for `min_length`. Maps to `-n`.
  required: false
  flag: -n
  format: flag
- name: max_length
  type: int
  description: Value for `max_length`. Maps to `-m`.
  required: false
  flag: -m
  format: flag
- name: additional_args
  type: string
  description: Value for `additional_args`.
  required: false
  format: positional
