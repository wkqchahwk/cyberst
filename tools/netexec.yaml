name: netexec
command: netexec
enabled: true
requires_action_enabled: true
short_description: Run the netexec security tool.
description: 'CyberStrikeAI wrapper for `netexec`. Command: `netexec`. Review scope and arguments before execution.'
parameters:
- name: protocol
  type: string
  description: Value for `protocol`.
  required: false
  default: smb
  position: 0
  format: positional
- name: target
  type: string
  description: Value for `target`.
  required: true
  position: 1
  format: positional
- name: username
  type: string
  description: Value for `username`. Maps to `-u`.
  required: false
  flag: -u
  format: flag
- name: password
  type: string
  description: Value for `password`. Maps to `-p`.
  required: false
  flag: -p
  format: flag
- name: hash_value
  type: string
  description: Value for `hash_value`. Maps to `-H`.
  required: false
  flag: -H
  format: flag
- name: module
  type: string
  description: Value for `module`. Maps to `-M`.
  required: false
  flag: -M
  format: flag
- name: additional_args
  type: string
  description: Value for `additional_args`.
  required: false
  format: positional
