name: mimikatz
command: mimikatz.exe
enabled: false
requires_action_enabled: true
short_description: Run the mimikatz security tool.
description: 'CyberStrikeAI wrapper for `mimikatz`. Command: `mimikatz.exe`. Review scope and arguments before execution.'
parameters:
- name: command
  type: string
  description: Value for `command`.
  required: true
  format: positional
- name: additional_args
  type: string
  description: Value for `additional_args`.
  required: false
  format: positional
