name: api-schema-analyzer
command: spectral
args:
- lint
enabled: true
short_description: Run the api-schema-analyzer security tool.
description: 'CyberStrikeAI wrapper for `api-schema-analyzer`. Command: `spectral`. Review scope and arguments before execution.'
parameters:
- name: schema_url
  type: string
  description: Value for `schema_url`.
  required: true
  position: 0
  format: positional
- name: additional_args
  type: string
  description: Value for `additional_args`.
  required: false
  format: positional
