name: steghide
command: steghide
enabled: true
short_description: Run the steghide security tool.
description: 'CyberStrikeAI wrapper for `steghide`. Command: `steghide`. Review scope and arguments before execution.'
parameters:
- name: action
  type: string
  description: Value for `action`.
  required: true
  position: 0
  format: positional
- name: cover_file
  type: string
  description: Value for `cover_file`. Maps to `-cf`.
  required: false
  flag: -cf
  format: flag
- name: embed_file
  type: string
  description: Value for `embed_file`. Maps to `-ef`.
  required: false
  flag: -ef
  format: flag
- name: passphrase
  type: string
  description: Value for `passphrase`. Maps to `-p`.
  required: false
  flag: -p
  format: flag
- name: stego_file
  type: string
  description: Value for `stego_file`. Maps to `-sf`.
  required: false
  flag: -sf
  format: flag
- name: additional_args
  type: string
  description: Value for `additional_args`.
  required: false
  format: positional
