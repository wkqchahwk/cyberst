name: gau
command: gau
enabled: true
short_description: Run the gau security tool.
description: 'CyberStrikeAI wrapper for `gau`. Command: `gau`. Review scope and arguments before execution.'
parameters:
- name: domain
  type: string
  description: Value for `domain`.
  required: true
  position: 0
  format: positional
- name: providers
  type: string
  description: Value for `providers`. Maps to `-providers`.
  required: false
  flag: -providers
  format: flag
- name: include_subs
  type: bool
  description: Value for `include_subs`. Maps to `-subs`.
  required: false
  flag: -subs
  format: flag
  default: true
- name: additional_args
  type: string
  description: Value for `additional_args`.
  required: false
  format: positional
