name: enum4linux
command: enum4linux
enabled: true
short_description: Run the enum4linux security tool.
description: 'CyberStrikeAI wrapper for `enum4linux`. Command: `enum4linux`. Review scope and arguments before execution.'
parameters:
- name: target
  type: string
  description: Value for `target`.
  required: true
  position: 0
  format: positional
- name: additional_args
  type: string
  description: Value for `additional_args`.
  required: false
  default: -a
  format: positional
