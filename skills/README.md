# Skills

Each skill directory contains a `SKILL.md` file and optional supporting resources. Keep package names stable and write concise, reusable instructions.

Recommended role-linked skills in this repository:

- `approved-web-validation`: controlled web and API validation for explicitly approved internal targets.
- `cleanup-and-rollback`: remove temporary test artifacts and roll back approved changes without tampering with audit evidence.
